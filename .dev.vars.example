# Copy this file to .dev.vars for local development with wrangler dev.
# .dev.vars is gitignored — never commit it.
JWKS_URL=http://localhost:8787/dev/jwks
JWT_ISSUER=http://localhost:8787
ENVIRONMENT=development
