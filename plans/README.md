# Implementation Plans

Plans 001-004: generated by the improve skill on 2026-06-23, audited against
commit `ad08a7d` (standard effort, direct read — baseline verified:
`npm run typecheck` clean, `npm test` = 310 passing).
Plans 005-008: generated by a second improve round on 2026-07-02, audited
against commit `431ac33` (standard effort, focus: architecture / readability /
stale code, joint audit with the sibling frontend `../UFCIM-FRONT3D` — its
plans live in that repo's `plans/`).
Plan 009: generated 2026-07-07 against commit `09fe163` from the QA kanban
board (`D:\Nextcloud\...\UFCIM-QA\00 - Kanban Geral.md`, card BUG-005); the
same round wrote frontend plans 001-007 in `../UFCIM-FRONT3D/plans/` for the
board's frontend bugs.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Recurring reservations exempt from active-reservation cap | P1 | S | — | DONE (branch `fix/recurring-active-limit`, reviewed 2026-06-23; verified on `09fe163` 2026-07-07) |
| 002  | Push admin list filtering & pagination into SQL | P2 | M | — | DONE (branch `perf/admin-list-sql-pagination`, reviewed 2026-06-23; verified on `09fe163` 2026-07-07) |
| 003  | Double-booking DB guard + correct false doc claim | P2 | M | — | DONE (branch `fix/double-booking-guard`, reviewed 2026-06-23; verified on `09fe163` 2026-07-07) |
| 004  | Hourly occupancy grid — DB-enforced non-overlap | P2 | L | 003 (supersedes) | ⛔ TRIGGER-GATED (gate re-checked 2026-07-07: still single-dept POC, no trigger) |
| 005  | Fix `listPending` filtering after SQL pagination (dropped rows) | P1 | S | — | DONE (branch `fix/pending-reports-pagination`, reviewed 2026-07-02; merged to main, verified on `09fe163` 2026-07-07) |
| 006  | Push audit-log list filtering & pagination into SQL | P1 | S | — | DONE (branch `perf/audit-log-sql-pagination`, reviewed 2026-07-02; merged to main, verified on `09fe163` 2026-07-07) |
| 007  | Consistency sweep (error classes, route validation, 4 duplicated maps/helpers) | P2 | M | — | DONE (branch `refactor/consistency-sweep`, reviewed 2026-07-02; merged to main, verified on `09fe163` 2026-07-07) |
| 008  | Remove four endpoints with zero consumers | P3 | S | — | DONE (branch `chore/remove-dead-endpoints`, reviewed 2026-07-02; merged to main, verified on `09fe163` 2026-07-07) |
| 009  | Reject same-day past-hour reservations (kanban BUG-005) | P1 | S | — | DONE (branch `fix/reject-past-hour-reservation`, executed by opencode/Qwopus-27B-Coder, reviewed 2026-07-08 — typecheck clean, 342 tests pass incl. 5 new, revert-proof verified) |

**004 is trigger-gated — do not execute on the normal backlog.** It runs only
when a second department with real users goes live on D1, or a real double-booking
is observed (with a Postgres-migration off-ramp). Full rationale and the
3-stage strategy: [`docs/adr/0001-reservation-overlap-enforcement.md`](../docs/adr/0001-reservation-overlap-enforcement.md).
Its read-path migration (Phase 2b) is intentionally deferred — see
[`docs/ROADMAP.md`](../docs/ROADMAP.md) item 1.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- 005/006/007/008 are mutually independent; 005 and 006 first (a real bug and
  the fastest-growing table, respectively). If 006 and 007 run concurrently,
  note both touch nothing in common — safe.
- No hard dependencies between 001/002/003.
- **Soft ordering**: 001 and 003 both edit `src/services/reservation.service.ts`.
  If running both, do **001 first** (it only deletes one line) to avoid a merge
  conflict in the same method region.
- 002 is fully independent (different methods / files).
- **004 supersedes 003**: it is the real fix (Stage 2 in ADR 0001). When 004
  executes, it replaces the Plan 003 partial index with the occupancy grid. 003
  is the POC stopgap that should already be live first.
- **009** is independent of all other backend plans. It shares semantics with
  frontend plan `../UFCIM-FRONT3D/plans/004-*` (both encode "the in-progress
  hour is still bookable") — if one side's rule changes, change the other.

## Decisions baked in

- **001**: Product owner confirmed (2026-06-23) that recurring series are
  *exempt* from the per-role active-reservation cap. The plan removes the
  misleading up-front check rather than enforcing the cap per-insert.

## Findings considered but not planned, and forward-looking direction

Audit findings that weren't turned into plans this round — plus product direction
options — now live in **[`docs/ROADMAP.md`](../docs/ROADMAP.md)** so there is one
home for "what's next" (and they aren't re-audited from scratch). Quick map:

- ROADMAP item 3 — public forgot-password flow.
- ROADMAP item 4 — auth hardening (login user-enumeration `auth.service.ts:40-54`;
  spoofable `X-Forwarded-For` fallback `rate-limit.ts:25-29`).
- ROADMAP item 5 — `/occupancy` missing date-range cap (`report.service.ts:122-125`
  caps `getSpaceReport` at 90 days; the `/occupancy` schema does not).
- ROADMAP item 2 — space visibility by scope (`GET /spaces` returns all spaces).
- Non-atomic blocking override (`blocking.service.ts:53-103`) is **absorbed by
  Plan 004** — the evict-then-claim batch makes the override atomic. No separate
  plan needed.

### From the 2026-07-02 round (considered, not planned)

- **`scripts/hermes-review/`** (LLM invitation-triage service + tests,
  invisible to typecheck/vitest via tsconfig/vitest include paths): flagged as
  orphaned by the audit, but it appears to be the maintainer's active local-LLM
  experiment — needs an owner decision (integrate into `src/` + CI, or park it
  outside the repo). Not planned; will bit-rot silently until decided.
- **`src/index.node.ts`** is a commented-out stub whose header claims "used in
  production (university server)" — false today; it's the aspirational
  Postgres-migration entry point (ROADMAP item 6 / overlap Stage 3). Fix the
  comment + the `docs/ARCHITECTURE.md:33` entry-points row when convenient
  (one-line doc edit, folded into no plan).
- **`PROD_ORIGINS` TODO in `src/app.ts:67-68`** lists `ufcim.pages.dev` while
  `wrangler.toml:49` says the frontend lives at `ufcim-front3d.pages.dev`.
  NOT a production outage: the frontend's `public/_worker.js` proxies
  `/api/v1/*` server-side, so browsers never make a cross-origin call and the
  CORS allowlist is inert. Align it (and remove the TODO) whenever `app.ts` is
  next touched; direct cross-origin API consumers would need it.
- **`createDb(c.env.DB)` boilerplate in ~83 handlers** (no `db` middleware):
  real duplication, M effort, wide mechanical diff — user selected the
  S-effort batch this round. Candidate for a future round.
- **`report.service.ts` god functions** (`getSpaceReport` ~265 lines;
  `getOccupancyReport` re-implements `calculateOccupancyRate` as a local
  `rate()` closure; unnamed turno cutoffs 13h/19h and 90-day cap): M effort,
  MED risk, needs characterization tests first. Candidate for a future round;
  partially overlaps the deferred Phase 2b read-path migration (ROADMAP item 1).
- **`conditions: any[]`** in `equipment-report.service.ts` /
  `report.service.ts`: cosmetic `any`, no runtime risk — not worth a plan.
- **`user-admin.service.ts` triplicated master-admin guard blocks** (3-4 lines
  ×3): too small to plan.

### Noted at reconcile (2026-07-07)

- **Role-label re-inlining in invitation admin views**: code merged after plan
  007's sweep re-inlines the title-case labels as `{ value, label }` option
  arrays instead of importing `ROLE_LABELS_TITLE`
  (`src/admin/views/invitation-requests.view.ts:19-24`,
  `src/admin/views/invitations.view.ts:71-76`). Two-line fix each, fold into
  the next touch of those files — too small to plan; does not reopen 007.
