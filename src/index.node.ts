// Node.js entry point — used in production (university server).
// Install @hono/node-server and swap src/db/client.ts to postgres-js before using.
//
// import { serve } from '@hono/node-server';
// import app from './app';
// serve({ fetch: app.fetch, port: 3000 });

export {};
